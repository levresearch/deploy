## Deploy

a client to dynamically deploy managed services on servers you own

### Build

Needs Go 1.26 or newer. `go build` targets the machine you run it on.

```
go build -o deploy .
./deploy -h
```
